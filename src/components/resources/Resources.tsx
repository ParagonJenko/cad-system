import React from 'react';
import { Container, Typography } from '@mui/material';

const Resources: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 3 }}>
        Resources
      </Typography>
      {/* Add your resources management logic here */}
    </Container>
  );
};

export default Resources;
